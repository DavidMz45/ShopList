<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShopList.Views.EstadisticasPage"
             Title="Estadísticas">
    <ScrollView>
        <VerticalStackLayout Spacing="16"
                              SemanticProperties.Description="Estadísticas de compras">
            <CollectionView ItemsSource="{Binding Statistics}"
                            SelectionMode="None">
                <CollectionView.EmptyView>
                    <StackLayout Padding="24"
                                 HorizontalOptions="Center">
                        <Label Text="No hay datos disponibles todavía." />
                    </StackLayout>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="12"
                               Margin="0,4"
                               CornerRadius="12"
                               BackgroundColor="{DynamicResource CardBackgroundColor}">
                            <Grid ColumnDefinitions="*,Auto">
                                <Label Text="{Binding Category}"
                                       FontAttributes="Bold" />
                                <Label Grid.Column="1"
                                       Text="{Binding Count, StringFormat={0} comprados}" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
