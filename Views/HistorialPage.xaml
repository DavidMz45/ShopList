<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShopList.Views.HistorialPage"
             Title="Historial">
    <ScrollView>
        <VerticalStackLayout Spacing="16"
                              SemanticProperties.Description="Historial de compras">
            <CollectionView ItemsSource="{Binding HistoryItems}"
                            SelectionMode="None">
                <CollectionView.EmptyView>
                    <StackLayout Padding="24"
                                 HorizontalOptions="Center">
                        <Label Text="Aún no hay historial disponible." />
                    </StackLayout>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="12"
                               Margin="0,4"
                               CornerRadius="12"
                               BackgroundColor="{DynamicResource CardBackgroundColor}">
                            <VerticalStackLayout Spacing="8">
                                <Label Text="{Binding Date, StringFormat='Fecha: {0:dd/MM/yyyy HH:mm}'}"
                                       FontAttributes="Bold" />
                                <FlexLayout Wrap="Wrap"
                                            BindableLayout.ItemsSource="{Binding Items}">
                                    <BindableLayout.ItemTemplate>
                                        <DataTemplate>
                                            <Frame Padding="4"
                                                   Margin="2"
                                                   BackgroundColor="{DynamicResource SecondaryColor}">
                                                <Label Text="{Binding Name}" />
                                            </Frame>
                                        </DataTemplate>
                                    </BindableLayout.ItemTemplate>
                                </FlexLayout>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
